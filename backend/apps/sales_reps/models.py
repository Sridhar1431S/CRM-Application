# Intentionally empty: "Sales Representative" is not a separate table.
# It is the apps.users.models.User model filtered to role=sales_rep.
# See apps/sales_reps/services.py and views.py for the domain logic,
# and README "Design Decisions" for the rationale.
